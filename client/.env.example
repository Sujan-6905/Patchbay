# Origin of the deployed signaling server (Render). Leave unset for local dev (Vite proxies
# /api and /socket.io to the local server) or any setup where the client and server share an
# origin. Required when the client is deployed separately, e.g. on Vercel.
# VITE_API_BASE_URL=https://patchbay-api.onrender.com
